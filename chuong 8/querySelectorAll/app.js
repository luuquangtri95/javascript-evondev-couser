/**
 * querySelectorAll =>> node list
 * chứa danh sách các node, nếu không có thì trả về empty
 *
 * nó có thể loop (không sử dụng method mảng) =>> array like object
 */

const multiNode = document.querySelectorAll('.item');
console.log(multiNode);

// Object Array (Array constructor)

/**
 * static method
 * Array.isArray(arr): kiểm tra arr có phải là mảng
 * Array.from(): tạo mảng mới từ các dữ liệu như SET...vv
 */

/**
 * kiểm tra phần tử tồn tại:
 * every(callback Fn)
 * some(callback Fn)
 * indexOf(search element)
 * lastIndexOf(search element)
 * includes(search element)
 * find(callback Fn)
 * findIndex(callback Fn)
 */

/**
 * thêm, xoá phần tử:
 * push(element0,element1...element n)   thêm cuối mảng
 * pop() : xoá cuối mảng
 * shift(): xoá đầu mảng
 * unshift(): thêm đầu mảng
 * splice(start, deleCount, item1... item n)  xoá/ thêm giữ mảng
 */

/**
 * nhóm hàm thường sử dụng
 * forEach()
 * map()
 * filter()
 * slice()
 * reduce()
 */

/**
 * 1 số ham khác
 * fill()
 * join()
 * concat()
 * reverse()
 * sort()
 */
